operations
checkout
	
	"A checkout operation loads all packages from this branch in the image.
	By default, it loads them from the latest commit of the branch.
	Then it sets the head to the checked out commit."
	
	repository checkoutCommitish: self